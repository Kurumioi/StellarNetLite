# 00. StellarNet Lite 文档总览与学习路径

这份文档用于给初学者建立一条连续的学习顺序，并说明当前文档集合已经覆盖了框架的哪些部分。

---

## 1. 当前推荐阅读顺序

当前文档顺序已经按“先主链、再业务、再同步、再表现、再扩展、再工具”的方式重排。

推荐顺序如下：

1. `01. StellarNet Lite 核心概念与全流程指南`
2. `02. StellarNet Lite 底层通讯与传输层指南`
3. `03. StellarNet Lite 全局模块与房间组件开发指南`
4. `04. StellarNet Lite 实体同步与动画同步深度解析`
5. `05. StellarNet Lite UI路由与表现层解耦指南`
6. `06. StellarNet Lite 回放重连与框架自定义指南`
7. `07. StellarNet Lite 零修改框架实现原功能拓展指南`
8. `08. StellarNet Lite 代码生成与编辑器工具链指南`
9. `09. StellarNet Lite 无头模式服务端命令使用指南`
10. `10. StellarNet Lite 服务端异步持久化工具使用指南`

---

## 2. 为什么这样排序

### 第一阶段：先看主链

先读：

1. `01`
2. `02`
3. `03`

这一阶段解决的是：

- 框架怎么启动。
- 服务端和客户端怎么跑起来。
- KCP / TCP / UDP 怎么接到统一接口。
- 协议为什么会被分发到模块和房间组件。

### 第二阶段：再看房间运行和表现

再读：

4. `04`
5. `05`

这一阶段解决的是：

- 房间里的实体怎么同步。
- 动画怎么同步。
- 为什么 UI 不直接写进 Runtime。
- 在线房间和回放房间为什么要拆路由。

### 第三阶段：最后看高级能力、工具链和运维工具

最后读：

6. `06`
7. `07`
8. `08`
9. `09`
10. `10`

这一阶段解决的是：

- 回放为什么能 Seek。
- 重连为什么能恢复房间。
- 如何在尽量不改 Runtime 的前提下扩展业务。
- Editor 代码生成链如何参与自动装配。
- 无头服务端如何查看状态、日志和执行停机。
- 服务端异步持久化任务如何登记、观察和在 `exit` 时等待。

---

## 3. 当前文档已经覆盖哪些主题

当前文档已经覆盖：

- 框架主流程
- 传输层
- 全局模块
- 房间组件
- 实体同步
- 动画同步
- UI 路由
- 回放
- 重连
- 扩展方式
- Editor 工具链
- 无头模式服务端命令
- 服务端异步持久化工具

对初学者来说，这已经足够支撑以下目标：

1. 跑通 Demo。
2. 看懂一条完整消息链。
3. 新增一个模块或房间组件。
4. 使用框架内置的实体同步链。
5. 看懂回放和重连的接入方式。
6. 在无头模式下排查服务端状态和错误日志。
7. 为需要的数据读写接入最小异步持久化工具。

---

## 4. 文档和代码如何对照阅读

为了避免“文档讲一套，代码是另一套”，建议按下面的对应关系一起看：

- `01` 对照 `GameLauncher`、`StellarNetAppManager`、`ServerApp`、`ClientApp`
- `02` 对照 `Runtime/Transports/*`
- `03` 对照 `Runtime/Server/Modules`、`Runtime/Client/Modules`、`Runtime/Server/Components`、`Runtime/Client/Components`
- `04` 对照 `ServerObjectSyncComponent`、`ClientObjectSyncComponent`、`ObjectSpawnerView`、`NetTransformView`、`NetAnimatorView`
- `05` 对照 `GlobalUIRouter`、`RoomViewManager`、`ClientRoomUIRouterBase`、`GameDemo/Client/View/UIPanel`
- `06` 对照 `ClientReplayPlayer`、`ReplaySnapshotDefines`、`ServerReplayStorage`
- `07` 对照 `GameDemo` 里的自定义业务组件和协议
- `08` 对照 `Editor/*.cs`
- `09` 对照 `Runtime/Server/Infrastructure/HeadlessServerLogger.cs`
- `10` 对照 `Runtime/Server/Core/ServerApp.cs`、`Runtime/Server/Infrastructure/ServerPersistenceRuntime.cs`、`Runtime/Server/Infrastructure/HeadlessServerLogger.cs`

---

## 5. 旧版冗余文档怎么处理

之前独立存在的“注释规范”和“当前实现对齐说明”类型文档已经不再保留为单独教学文档。

现在的做法是：

- 注释规范直接落实到代码中。
- 实现对齐直接落实到各主题文档中。

这样更适合初学者顺着主线学习，也避免文档数量多但内容分散。

---

## 6. 初学者建议的实际上手顺序

如果你第一次接触这个仓库，建议按下面顺序动手：

1. 先看 `GameLauncher` 和 `StellarNetAppManager`。
2. 再看 `ServerRoomModule` 和 `ClientRoomModule`。
3. 然后看 `ServerRoomSettingsComponent` 和 `ClientRoomSettingsComponent`。
4. 再看 `ServerObjectSyncComponent` 和 `ClientObjectSyncComponent`。
5. 最后再看 `GameDemo` 的 Router、Panel、View。

按这个顺序，理解成本最低。
